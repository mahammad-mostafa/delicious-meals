const popup_section = document.getElementById('popup_section');
const close_button = document.getElementById('close_button');
const meal_image = document.getElementById('meal_image');
const meal_title = document.getElementById('meal_title');
const meal_description = document.getElementById('meal_description');
const sub_title_action = document.getElementById('sub_title_action');
const date_list = document.getElementById('date_list');
const add_action_title = document.getElementById('add_action_title');
const comment_form = document.getElementById('comment_form');
const reservation_form = document.getElementById('reservation_form');
const comment_username = document.getElementById('comment_username');
const comment_area = document.getElementById('comment_area');
const submit_comment_btn = document.getElementById('submit_comment_btn');
const reservation_username = document.getElementById('reservation_username');
const reservation_date_start = document.getElementById('reservation_date_start');
const reservation_date_end = document.getElementById('reservation_date_end');
const submit_reservation_btn = document.getElementById('submit_reservation_btn');

export {
    popup_section,
    close_button,
    meal_image,
    meal_title,
    meal_description,
    sub_title_action,
    date_list,
    add_action_title,
    comment_form,
    reservation_form,
    comment_username,
    comment_area,
    submit_comment_btn,
    reservation_username,
    reservation_date_start,
    reservation_date_end,
    submit_reservation_btn
};